namespace IRunes.App.ViewModels.Widgets
{
    using SIS.MvcFramework.ViewEngine;

    //we need the location of the class, so we can get the location of the view!, because .html don`t have types!
    //you can`t go to the html file with reflection, that is why we need another class, for direction
    public class ValidationWidget : ViewWidget
    {

    }
}
